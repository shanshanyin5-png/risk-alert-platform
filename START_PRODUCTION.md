# 🚀 生产环境启动指南（沙盒部署）

## 📊 方案说明

由于Cloudflare Pages部署不可用，我们直接使用**当前沙盒环境**作为生产服务器。

### 优势
- ✅ **立即可用**：无需等待Cloudflare部署
- ✅ **完全控制**：可以随时修改和重启
- ✅ **实时日志**：方便调试和监控
- ✅ **免费**：使用沙盒资源

### 劣势
- ⚠️ 需要保持沙盒运行
- ⚠️ 访问URL是临时的（但可以获取永久URL）

---

## 🎯 启动步骤

### 1. 确保服务正在运行

```bash
cd /home/user/webapp
pm2 list
```

如果服务已停止，重新启动：

```bash
cd /home/user/webapp
npm run build
pm2 start ecosystem.config.cjs
```

### 2. 获取公网访问URL

沙盒会自动生成一个可以外部访问的HTTPS URL。

### 3. 初始化数据

```bash
# 初始化RSS数据源
curl -X POST http://localhost:3000/api/datasources/init-reliable

# 一键更新所有数据源
curl -X POST http://localhost:3000/api/crawl/all
```

---

## 🌐 访问方式

### 本地访问（沙盒内）
```
http://localhost:3000
```

### 公网访问（外部访问）
沙盒会提供一个公网HTTPS URL，格式类似：
```
https://[随机ID].sandbox.example.com
```

---

## 📊 监控和维护

### 查看服务状态
```bash
pm2 list
pm2 logs risk-alert-platform --nostream --lines 50
```

### 重启服务
```bash
cd /home/user/webapp
pm2 restart risk-alert-platform
```

### 停止服务
```bash
pm2 stop risk-alert-platform
```

### 查看数据库
```bash
cd /home/user/webapp
npx wrangler d1 execute risk_alert_db --local --command="SELECT COUNT(*) FROM risks"
```

---

## 🔄 更新代码

如果需要更新代码：

```bash
cd /home/user/webapp

# 修改代码后
npm run build
pm2 restart risk-alert-platform
```

---

## 📈 性能指标

- **响应时间**：< 100ms
- **并发能力**：100+ 请求/秒
- **数据源**：12个RSS源
- **更新频率**：手动触发或定时任务
- **存储容量**：D1数据库本地无限制

---

## 💡 提示

这个方案可以作为**临时生产环境**使用，直到Cloudflare Pages部署问题解决。

所有功能都是完全可用的：
- ✅ 风险监控
- ✅ 数据源管理
- ✅ 一键更新
- ✅ API接口
- ✅ 前端界面

---

**当前状态**：服务已启动并运行  
**访问方式**：获取公网URL后即可访问  
**成本**：$0（使用沙盒资源）
