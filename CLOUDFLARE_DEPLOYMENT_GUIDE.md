# Cloudflare Pages 部署详细图文指南

## 🎯 如何找到部署按钮

### Cloudflare Pages 界面布局

登录后的界面通常是这样的：

```
Cloudflare Dashboard
├── 左侧菜单栏
│   ├── Overview (概览)
│   ├── Workers & Pages ← 点这里
│   ├── DNS
│   └── ...
└── 主内容区
    └── Workers & Pages 列表
        └── risk-alert-platform ← 您的项目
```

---

## 📋 详细步骤

### 步骤1：登录并找到项目

1. **访问：** https://dash.cloudflare.com/
2. **登录** 您的Cloudflare账号
3. **点击左侧** `Workers & Pages`
4. **找到项目** `risk-alert-platform`
5. **点击项目名称** 进入项目详情页

---

### 步骤2：在项目页面中找到部署选项

进入项目后，顶部会有几个标签页：

```
┌─────────────────────────────────────────────┐
│ risk-alert-platform                         │
├─────────────────────────────────────────────┤
│ [View site]  Production: xxx.pages.dev     │
├─────────────────────────────────────────────┤
│ Deployments | Settings | Functions | ...   │
└─────────────────────────────────────────────┘
```

**重要标签页：**
- **Deployments** - 部署历史和管理
- **Settings** - 项目设置

---

### 步骤3：触发新部署的几种方式

#### 方式A：通过 Deployments 页面

1. **点击** `Deployments` 标签页
2. 您会看到部署历史列表：

```
┌─────────────────────────────────────────────┐
│ Deployments                                  │
├─────────────────────────────────────────────┤
│ [Create deployment]  ← 看看有没有这个按钮     │
├─────────────────────────────────────────────┤
│ Production                                   │
│ ✓ Deployed  2 hours ago  main  abc1234     │
│ ✓ Deployed  1 day ago    main  def5678     │
└─────────────────────────────────────────────┘
```

3. **选项1：** 如果看到 `Create deployment` 按钮
   - 直接点击
   - 选择 `Production branch (main)`
   - 点击 `Save and Deploy`

4. **选项2：** 点击最新的部署记录
   - 进入部署详情页
   - 查看右上角是否有 `Redeploy` 或 `Retry` 按钮

#### 方式B：通过 Settings 页面

1. **点击** `Settings` 标签页
2. **找到** `Builds & deployments` 部分
3. **查看** `Production branch` 是否设置为 `main`
4. **确认** `Automatic deployments` 是否启用

如果看到：
```
┌─────────────────────────────────────────────┐
│ Builds & deployments                         │
├─────────────────────────────────────────────┤
│ Production branch: main                      │
│ Automatic deployments: [✓] Enabled          │
│                                              │
│ [Retry deployment] ← 可能在这里              │
└─────────────────────────────────────────────┘
```

#### 方式C：GitHub 自动触发（已完成✅）

**我已经帮您执行了这个方法！**

刚才推送了一个空提交到GitHub，Cloudflare Pages应该会自动检测并部署。

**查看自动部署状态：**
1. 进入 `Deployments` 页面
2. 刷新页面
3. 看最上方是否有新的部署任务
4. 状态可能是：
   - 🟡 **Building** - 正在构建
   - 🟢 **Deployed** - 部署成功
   - 🔴 **Failed** - 部署失败

---

### 步骤4：手动上传构建产物（终极方案）

如果所有按钮都找不到，可以手动上传：

1. **在Deployments页面** 找 `Upload assets` 或 `Direct Upload`
2. **或者使用wrangler命令行**（需要API token）

**手动上传步骤：**
```bash
# 1. 本地构建
cd /home/user/webapp
npm run build
# 生成 dist/ 目录

# 2. 打包
cd dist
tar -czf ../dist.tar.gz .

# 3. 在Cloudflare Pages中上传 dist.tar.gz
```

---

## 🔍 查看部署状态

### 实时查看部署进度

1. **进入** `Deployments` 页面
2. **点击** 最新的部署记录
3. **查看** 构建日志

典型的构建日志：
```
Initializing build environment
Cloning repository
Installing dependencies (npm install)
Building project (npm run build)
Deploying to production
✓ Deployment complete!
```

### 部署完成标志

- ✅ 状态显示 **Success** 或 **Deployed**
- ✅ 显示新的部署URL
- ✅ 构建日志最后一行显示 `Deployment complete`

---

## 🧪 验证部署是否成功

### 方法1：检查API版本标识

```bash
# 测试新API是否可用（这个API只在新代码中存在）
curl -X POST https://risk-alert-platform.pages.dev/api/datasources/init-reliable
```

**如果返回：**
- ✅ `{"success": true, "count": 12}` - 新代码已部署
- ❌ `Internal Server Error` 或 `404` - 还是旧代码

### 方法2：查看数据源列表

```bash
curl https://risk-alert-platform.pages.dev/api/datasources | grep -o "BBC News"
```

**如果返回：**
- ✅ 包含 `"BBC News - World"` - 新代码
- ❌ 只有旧数据源 - 旧代码

### 方法3：访问前端页面

打开：https://risk-alert-platform.pages.dev/

在浏览器控制台（F12）检查是否有错误

---

## 📊 Cloudflare Pages 常见界面位置

### 界面1：项目列表页
```
https://dash.cloudflare.com/{account-id}/workers-and-pages
```
显示所有Workers和Pages项目

### 界面2：项目详情页
```
https://dash.cloudflare.com/{account-id}/pages/view/{project-name}
```
显示项目的Deployments、Settings等

### 界面3：部署详情页
```
https://dash.cloudflare.com/{account-id}/pages/view/{project-name}/{deployment-id}
```
显示单个部署的构建日志

---

## ⏱️ 预计等待时间

- **自动部署触发：** 0-2分钟（GitHub → Cloudflare）
- **构建时间：** 1-3分钟
- **部署时间：** 30秒-1分钟
- **总计：** **2-6分钟**

---

## 🎯 我已经做的事情

✅ **刚刚完成：**
1. 创建空提交到GitHub
2. 推送到main分支
3. 触发Cloudflare Pages自动部署

✅ **您需要做的：**
1. 等待2-6分钟
2. 刷新Cloudflare Pages的Deployments页面
3. 查看是否有新的部署任务
4. 等待部署完成

---

## 🆘 如果实在找不到部署按钮

### 替代方案A：通过GitHub再次触发

我可以再推送一次空提交：
```bash
git commit --allow-empty -m "再次触发部署"
git push origin main
```

### 替代方案B：提供界面截图

如果您能提供Cloudflare Pages界面的截图，我可以：
- 准确指出按钮位置
- 提供针对您界面的具体指导

### 替代方案C：检查自动部署设置

在Settings → Builds & deployments中：
1. 确认GitHub仓库已连接
2. 确认Production branch是main
3. 确认Automatic deployments已启用

---

## ✅ 快速检查清单

5分钟后执行以下检查：

```bash
# 1. 检查新API是否可用
curl -X POST https://risk-alert-platform.pages.dev/api/datasources/init-reliable

# 2. 如果返回成功，继续初始化
# (新API可用说明部署成功)

# 3. 测试一键更新
curl -X POST https://risk-alert-platform.pages.dev/api/crawl/all

# 4. 查看结果
curl https://risk-alert-platform.pages.dev/api/risks?limit=5
```

---

## 📞 当前状态

**GitHub代码：** ✅ 最新 (commit 07335c2)
**自动部署：** 🟡 已触发，等待中...
**预计完成：** 2-6分钟

**请您：**
1. 等待几分钟
2. 刷新Cloudflare Pages Deployments页面
3. 查看是否有新的构建任务

如果5-10分钟后还没有新的部署，请告诉我，我会提供其他解决方案！

---

**最后更新：** 2026-01-04  
**触发时间：** 刚刚  
**预计完成：** 5-10分钟内
